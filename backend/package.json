{
  "name": "jukebox-d-backend",
  "version": "0.0.0",
  "scripts": {
    "build": "python -m venv venv && .\\venv\\Scripts\\activate && pip install -r requirements.txt",
    "start": ".\\venv\\Scripts\\activate && python app.py"
  }
} 